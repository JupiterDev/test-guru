<tr>
	<td><%= badge.title %></td>
	<td><%= badge.pic_url %></td>
	<td><%= badge.rule %></td>
	<td><%= badge.criterion %></td>
	<td>
		<%= link_to t('link.edit'), edit_admin_badge_path(badge)%>
  </td>
	<td>
    <%= link_to t('link.delete'), 
                admin_badge_path(badge),
                method: :delete,
                data: { confirm: "Are you sure?" } %>
  </td>
</td>
